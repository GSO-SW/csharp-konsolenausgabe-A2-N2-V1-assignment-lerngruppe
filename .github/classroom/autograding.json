{
  "tests": [
    {
      "name": "Task1_Messdaten",
      "setup": "sudo apt-get update && sudo apt-get install -y apt-transport-https && sudo apt-get update && sudo apt-get install -y dotnet-sdk-6.0",
      "run": "DOTNET_CLI_HOME=/tmp/ dotnet test --filter ClassName=Tests_Aufgabe1.UnitTest1",
      "input": "",
      "output": "",
      "comparison": "included",
      "timeout": 5,
      "points": 50
    },
    {
      "name": "Task2_Gehaltsliste",
      "setup": "sudo apt-get update && sudo apt-get install -y apt-transport-https && sudo apt-get update && sudo apt-get install -y dotnet-sdk-6.0",
      "run": "DOTNET_CLI_HOME=/tmp/ dotnet test --filter ClassName=Tests_Aufgabe2.UnitTest2",
      "input": "",
      "output": "",
      "comparison": "included",
      "timeout": 5,
      "points": 50
    }

  ]
}